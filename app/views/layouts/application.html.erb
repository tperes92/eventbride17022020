<!DOCTYPE html>
<html>
  <head>
    <title>Eventbride</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  </head>

  <body>
    <header class="fixed-top">
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary" style="height: 4rem">
        <a class="navbar-brand" href="<%= root_path %>">Eventbride</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse bg-primary navbar-dark" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <li class="nav-item <%="active" if current_page? root_path %>">
              <a class="nav-link" href="<%= root_path %>">Accueil <span class="sr-only"><%="(actuel)" if current_page? root_path %></span></a>
            </li>
            <li class="nav-item <%="active" if current_page? new_event_path %>">
              <a class="nav-link" href="<%= new_event_path %>">Créer un évènement <span class="sr-only"><%="(actuel)" if current_page? new_event_path %></span></a>
            </li>
            <% unless user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle <%= "active" if current_page? new_user_registration_path %><%= "active" if current_page? new_user_session_path %>" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  S'inscrire / Se connecter
                </a>
                <div class="dropdown-menu bg-primary " aria-labelledby="navbarDropdownMenuLink">
                  <a class="dropdown-item text-white" href="<%= new_user_registration_path %>">S'inscrire</a>
                  <a class="dropdown-item text-white" href="<%= new_user_session_path %>">Se connecter</a>
                </div>
              </li>
            <% else %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle <%= "active" if current_page? user_path(current_user.id) %>" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Mon profil
                </a>
                <div class="dropdown-menu bg-primary" aria-labelledby="navbarDropdownMenuLink">
                  <a class="dropdown-item text-white" href="<%= user_path(current_user.id) %>">Mon profil</a>
                  <%= link_to "Se deconnecter", destroy_user_session_path, method: :delete, class:"dropdown-item text-white"%>
                </div>
              </li>
            <% end %>
          </ul>
        </div>
      </nav>
    </header>
    <main style="margin-top: 4rem;">
      <%= yield %>
    </main>
    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
  </body>
</html>
